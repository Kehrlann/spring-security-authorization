spring:
  application:
    name: authorization-demo

server:
  # faster reboot
  address: 127.0.0.1
  port: 8080
  servlet:
    session:
      persistent: true


logging:
  level:
    org.springframework.security: TRACE